[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "arm-kinematics-control-planning"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]
